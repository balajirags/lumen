Treat the repository as a backend service unless the graph clearly contradicts that assumption.

- Prefer business capabilities, entry points, integration boundaries, persistence, and decomposition seams.
- Use bounded-context and strangler-fig language when supported by current-state evidence.
- Generate API and ER artifacts only when the graph provides enough evidence.
- Use the C4 diagram tool for C4 context/target diagrams; use Mermaid for ER and journey diagrams.
