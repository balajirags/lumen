Treat the repository as a frontend application.

- Prefer routes, screens, components, hooks, state boundaries, and API clients over backend-only concepts.
- Frame business journeys as user interaction flows.
- Do not force backend-only language such as aggregate roots, ER models, or strangler-fig extraction unless the graph clearly supports it.
- When recommending target state, focus on component/module boundaries, shared state isolation, and integration seams.
